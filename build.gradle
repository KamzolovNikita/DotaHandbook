buildscript {
  repositories {
    google()
    mavenCentral()
  }
  dependencies {
    Plugins.all().each { plugin ->
      classpath plugin
    }
  }
}
task clean(type: Delete) {
  delete rootProject.buildDir
}